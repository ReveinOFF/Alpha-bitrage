export enum Role {
  USER = 'user',
  ADMIN = 'admin',
}

export enum Status {
  PENDING = 'Pending',
  CANCELED = 'Canceled',
  SUCCESS = 'Success',
}

export enum HelpType {
  ANNOUNCEMENTS = 'Announcements',
  GUIDES = 'Guides',
}
